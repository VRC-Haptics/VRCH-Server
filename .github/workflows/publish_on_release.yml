name: 'publish'

on:
  workflow_dispatch:
  push:
    branches:
      - release

jobs:
  publish-tauri:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'windows-latest'
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v4

      - name: Install dependencies (Ubuntu only)
        if: matrix.platform == 'ubuntu-22.04' # This must match the platform value defined above.
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
            version: ^10

      - name: Install dotnet for sidecars
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: 8.0.x

      - name: compile dotnet dll
        run: dotnet publish "src-vrc-oscquery/listen-for-vrc/listen-for-vrc.csproj" -c Release -r "win-x64" -p:PublishAot=true -p:NativeLib=Shared -p:SelfContained=true -p:StripSymbols=true -p:IlcGenerateDebugInfo=false -p:IlcGenerateCompletePdb=false -o "src-tauri\sidecars"
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: lts/*
          cache: 'pnpm'

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Compile Proxy Shim
        run: cargo build --release --manifest-path "src-proxy/Cargo.toml" --target-dir "src-tauri\sidecars\bHapticsPlayer\BhapticsPlayer.exe"

      - name: Compile Elevated Sidecar
        run: cargo build --release --manifest-path "src-elevated-register/Cargo.toml" --target-dir "src-tauri\sidecars"

      - name: List sidecar files
        shell: pwsh
        run: dir "src-tauri\sidecars" -Recurse -File

      - name: Install frontend dependencies
        run: pnpm install

      - uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: app-v__VERSION__ # the action automatically replaces __VERSION__ with the app version.
          releaseName: 'App v__VERSION__'
          releaseBody: 'See the assets to download this version and install.'
          releaseDraft: true
          prerelease: false
          args: ${{ matrix.args }}

